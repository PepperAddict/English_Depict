.images-selection
  position: relative;
  width: auto;
  display: inline-block;
  &:hover
    button
      display: block;
  button
    display: none;
    position: absolute;
    width:100%;
    top: 0px;
    left: 0px;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    border: 0px;
    color: white;
    font-weight: 800;